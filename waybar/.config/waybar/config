{
    "layer": "top",
    "position": "top",
    "height": 28,
    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": ["sway/window"],
    "modules-right": ["tray", "custom/pacman", "temperature", "custom/spotify", "pulseaudio", "battery", "memory", "cpu", "network", "disk", "custom/power", "clock"],
    "sway/workspaces": {
        "disable-scroll": true,
        "format": "{name}"
    },
    "sway/mode": {
        "format": " {}"
    },
    "sway/window": {
        "max-length": 60
    },
    "clock": {
        "format": "{:%H:%M}",
        "format-alt": "{:%d-%m-%Y %H:%M:%S}",
        "interval": 1,
        "tooltip-format": "{:%A, %B %d, %Y}"
    },
    "cpu": {
        "format": " {usage}%",
        "interval": 2,
        "tooltip": true
    },
    "memory": {
        "format": " {used:0.1f}G",
        "interval": 2,
        "tooltip-format": "{used:0.1f}G / {total:0.1f}G"
    },
    "temperature": {
        "critical-threshold": 60,
        "format": "{icon} {temperatureC}°C",
        "format-icons": ["", "", ""]
    },
    "battery": {
        "states": {
            "warning": 20,
            "critical": 5
        },
        "format": "{icon} {capacity}%",
        "format-charging": " {capacity}%",
        "format-plugged": " {capacity}%",
        "format-icons": ["", "", "", "", ""]
    },
    "network": {
        "interface": "wl*",
        "format-wifi": " {essid}",
        "format-disconnected": " disconnected",
        "tooltip-format": "{ifname}: {ipaddr}/{cidr}"
    },
    "pulseaudio": {
        "format": " {volume}%",
        "format-muted": " muted",
        "on-click": "pavucontrol",
        "scroll-step": 5
    },
    "disk": {
        "format": " {free}",
        "path": "/"
    },
    "tray": {
        "spacing": 8
    },
    "custom/pacman": {
        "format": " {}",
        "exec": "checkupdates | wc -l",
        "interval": 1000,
        "signal": 8
    },
    "custom/spotify": {
        "format": " {}",
        "exec": "playerctl -p spotify metadata --format '{{artist}} - {{title}}'",
        "interval": 1,
        "on-click": "playerctl -p spotify play-pause",
        "escape": true
    },
    "custom/power": {
        "format": "",
        "on-click": "~/.config/sway/scripts/power-profiles",
        "tooltip": false
    }
}
