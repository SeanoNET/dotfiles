#!/bin/sh
eval $(/usr/bin/gnome-keyring-daemon --start)
export SSH_AUTH_SOCK
# Keep the fixed IdentityAgent path in ~/.ssh/config aimed at the live socket.
mkdir -p "$HOME/.ssh/agent" && ln -sfn "$SSH_AUTH_SOCK" "$HOME/.ssh/agent/current"
