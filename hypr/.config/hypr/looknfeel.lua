-- Personal look'n'feel overrides.
--
-- Tighter than Omarchy's defaults (gaps_in 5 / gaps_out 10 / border_size 2) to
-- match the sway config's `gaps inner 4`, `gaps outer 2` and
-- `default_border pixel 1`.

-- https://wiki.hypr.land/Configuring/Basics/Variables/#general
hl.config({
  general = {
    gaps_in = 4,
    gaps_out = 2,
    border_size = 1,
  },
})
